# Assets

Static brand and cockpit artwork (non-code):

- Logos, wordmarks, icon sets
- Reference mecha silhouette / HUD frames (PNG/SVG)
- Optional Lottie or Rive files for future avatar layers

Do **not** commit large binary packs without Git LFS. Prefer SVG + procedural effects in the frontend where possible.
