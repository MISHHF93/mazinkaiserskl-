# `voice/` (top-level package)

**Target:** server-side voice orchestration — STT/TTS providers, streaming bridges, audio session ids.

**Today:** browser handles STT/TTS; backend remains text-first. Add Whisper-compatible adapters here (Prompt 4).
