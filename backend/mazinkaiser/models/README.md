# `models/`

SQLAlchemy **ORM** models (Postgres) and Alembic migrations target directory.

Domain **Pydantic** models remain in `mazinkaiser.domain` for now; migrate only when persistence boundaries are clear.
